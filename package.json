{
  "name": "internal-api-mcp-server",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP Server for internal API access - protocol bridge between AI tools and auth backend",
  "main": "dist/index.js",
  "bin": {
    "internal-api-mcp-server": "dist/index.js"
  },
  "scripts": {
    "prepare": "tsc",
    "build": "tsc",
    "dev": "node dist/index.js",
    "test": "vitest --run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "open": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "vitest": "^3.0.0",
    "fast-check": "^4.0.0"
  }
}
